import 'package:flutter/material.dart';

class AppPadding {
  static const main = EdgeInsets.symmetric(horizontal: 24.0);
  static const card = EdgeInsets.all(16.0);
  static const button = EdgeInsets.symmetric(horizontal: 24.0, vertical: 12);

  static const cardMargin = EdgeInsets.only(top: 24.0);
}
